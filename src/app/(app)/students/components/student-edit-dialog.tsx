
"use client";

// This file is no longer used for student editing on the main student page.
// The editing functionality has been moved to EditStudentForm.tsx, which is an inline form.
// This file can be safely deleted if not used elsewhere in the project.

import * as React from "react";

export function StudentEditDialog_DEPRECATED() {
  return (
    <div>
      Este componente (StudentEditDialog) ha sido reemplazado por EditStudentForm.
    </div>
  );
}
